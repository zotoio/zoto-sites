services:
  nginx:
    build: .
    restart: always
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./backends/botz.ai/cache:/usr/share/nginx/html/botz.ai/cache:ro
      # LE copies for future vhost use; mount avoids /etc/nginx/ssl so the image can write its default self-signed cert at startup.
      - ${SSL_CERT_DIR:-./ssl}:/etc/nginx/certs:ro
    depends_on:
      - botz

  botz:
    build: ./backends/botz.ai
    restart: always
    container_name: botz
    env_file:
      - ./backends/botz.ai/.env
    environment:
      CACHE_DIR: /home/root/cache
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./backends/botz.ai/cache:/home/root/cache

  discord:
    build: ./backends/discord
    restart: always
    container_name: discord
    env_file:
      - ./backends/discord/.env
